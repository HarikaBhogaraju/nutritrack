# Lower-bound pins. Pip will install the latest compatible versions, which
# matters because some pinned-old wheels don't exist for very new Python
# versions (e.g. pydantic-core needed Rust compilation on Python 3.14 with
# the old pin and that build broke).
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
pydantic>=2.10.0
pydantic-settings>=2.6.0
bcrypt>=4.2.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.12
httpx>=0.27.2
anthropic>=0.39.0
email-validator>=2.2.0
